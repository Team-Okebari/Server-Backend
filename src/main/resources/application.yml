spring:
  application:
    name: artbite
  profiles: { } # 활성 프로필은 실행 시점에 명시적으로 지정 (예: --spring.profiles.active=local)
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME}
    hikari: # HikariCP specific properties
      minimum-idle: 1
      maximum-pool-size: 10
      connection-timeout: 30000
      auto-commit: true
  h2:
    console:
      enabled: true
  jpa:
    # database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: update # 개발 환경 기본값, 프로필별로 오버라이드
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
      port: ${SPRING_DATA_REDIS_PORT}
server:
  port: 8080
jwt:
  secret: ${JWT_SECRET_KEY}
  access-token-expire-time: ${JWT_ACCESS_TOKEN_EXPIRE_TIME}
  refresh-token-expire-time: ${JWT_REFRESH_TOKEN_EXPIRE_TIME}